# task_manager_api 

This is a django application that allows only users connected to create, retrieve, update, and delete his owns tasks changing their status in the app
also to display tasks history the app has the API that can allow client to communicate with the app.
